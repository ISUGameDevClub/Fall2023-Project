using UnityEngine;

public class TwoWayPlatform : MonoBehaviour
{
    //work on the moving platform before continuing on this twowayplatform-c
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
